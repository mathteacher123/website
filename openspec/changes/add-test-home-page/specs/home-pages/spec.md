## ADDED Requirements

### Requirement: Test Home Page

The system SHALL provide a home page variant accessible at /homes/test that displays only a hero section.

#### Scenario: Page displays hero only

- **WHEN** user navigates to /homes/test
- **THEN** only the hero section is rendered
- **AND** no other page sections are present
